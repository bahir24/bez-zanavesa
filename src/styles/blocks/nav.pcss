.nav {
  width: 100%;
  @include tablets {
    display:none;
  }
}

.nav__list {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.nav__item {
  // margin-right: 2%;
  &:last-child {
    margin-right: 0;
  }
}

.nav__link {
  text-decoration: none;
  font-size: 16px;
  text-transform: uppercase;
  color: #fff;
}