.nav {
  width: 70%;
}

.nav__list {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.nav__item {
  &:last-child {
    margin-right: 0;
  }
}

.nav__link {
  text-decoration: none;
  font-size: 16px;
  text-transform: uppercase;
  color: #fff;
}