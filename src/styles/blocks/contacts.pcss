.contacts {
  // height: 100vh;
  background-color: #f2eded;
}

.contacts__text {
  font-size: 48px;
  font-weight: 700;
  color: #000;
  text-align: center;
  margin-bottom: 40px;
  @include phones {
    font-size: 25px;
  }
}